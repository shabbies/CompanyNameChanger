<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Company Name Changer</title>

    <!-- Bootstrap -->

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <div class="content">
      <h1>This changes your company name</h1>
      <p> Please ensure that the old company name is exactly the same (correct case, spacing) for the program to work </p>
      <%= simple_form_for Message.new, :url => new_message_path, :method => :get do |f| %>
      <%= f.input(:oldcompany, label: "The Old Company Name") %>
      <%= f.input(:newcompany, label: "The New Company Name") %>
      <%= f.input(:message, as: :text, label: "Your Text") %>
      <%= f.button :submit %>
      <% end %>
    </div>
  </body>
</html>